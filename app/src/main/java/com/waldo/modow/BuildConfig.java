package com.waldo.modow;

/**
 * Versión visible usada por el formato de backup. El proyecto actual no genera
 * BuildConfig automáticamente, por eso se mantiene este valor junto al versionName.
 */
public final class BuildConfig {
    public static final String VERSION_NAME = "1.5.1";
    private BuildConfig() {}
}
